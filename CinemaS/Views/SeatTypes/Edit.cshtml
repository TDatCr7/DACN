@model CinemaS.Models.SeatTypes
@{
    ViewData["Title"] = "Chỉnh sửa loại ghế";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="admin-shell admin-form-wrap">
    <div class="admin-panel">

        <div class="admin-toolbar" style="margin-bottom:10px;">
            <div style="display:flex;align-items:center;gap:10px;">
                <h1 style="margin:0;font-weight:900;">
                    <i class="fa-solid fa-couch"></i>
                    @ViewData["Title"]
                </h1>
            </div>

            <div class="d-flex gap-2">
                <a asp-action="Index"
                   class="btn btn-outline-light-rounded btn-sweep px-3"
                   style="border-radius:999px; padding:9px 20px; font-size:14px; font-weight:800; white-space:nowrap;">
                    <i class="fa-solid fa-arrow-left"></i> Quay lại
                </a>

                <button type="submit"
                        form="seatTypeEditForm"
                        class="btn btn-primary btn-sweep px-3"
                        style="border-radius:999px; padding:9px 20px; font-size:14px; font-weight:900; white-space:nowrap;">
                    <i class="fa-solid fa-floppy-disk me-1"></i> Lưu thay đổi
                </button>
            </div>
        </div>

        <div style="height:1px;background:rgba(148,163,255,.18);margin:8px 0 14px;"></div>

        <div class="admin-form-card" style="background:transparent;border:none;box-shadow:none;padding:0;">
            <div class="mb-2" style="font-weight:900;color:#c7d2fe;letter-spacing:.3px;">
                Thông tin loại ghế
            </div>

            <form asp-action="Edit" method="post" id="seatTypeEditForm">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="SeatTypeId" />
                <div asp-validation-summary="ModelOnly" class="text-danger mb-2"></div>

                <div class="row g-3">
                    <div class="col-md-6">
                        <label asp-for="Name" class="form-label">Tên loại ghế</label>
                        <input asp-for="Name" class="form-control" />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>

                    <div class="col-md-6">
                        <label asp-for="Price" class="form-label">Giá vé</label>
                        <input asp-for="Price" type="number" min="0" class="form-control" />
                        <span asp-validation-for="Price" class="text-danger"></span>
                    </div>
                </div>
            </form>
        </div>

    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />

    <script>
        // Tự động ẩn thông báo sau 5 giây
        setTimeout(() => document.querySelectorAll('.alert').forEach(a => a.style.display = 'none'), 5000);

        // Kiểm tra form trước khi gửi
        document.addEventListener('DOMContentLoaded', () => {
            const form = document.querySelector('form');
            const name = document.getElementById('Name');
            const price = document.getElementById('Price');

            form.addEventListener('submit', e => {
                let errors = [];

                if (!name.value.trim()) errors.push("• Tên loại ghế không được để trống");
                if (!price.value || price.value <= 0) errors.push("• Giá vé phải lớn hơn 0");

                if (errors.length > 0) {
                    e.preventDefault();
                    alert("⚠️ Vui lòng kiểm tra lại:\n\n" + errors.join("\n"));
                }
            });
        });
    </script>
}
